# Secrets — DO NOT commit real values to git.
#
# Preferred: create via CLI (avoids secrets in YAML files):
#   oc create secret generic excel-copilot-secrets \
#     --from-literal=LLM_API_KEY='your-api-key-here'
#
# Or apply this template after replacing the placeholder:
apiVersion: v1
kind: Secret
metadata:
  name: excel-copilot-secrets
  labels:
    app: excel-copilot
    app.kubernetes.io/name: excel-copilot
    app.kubernetes.io/part-of: excel-ai-copilot
type: Opaque
stringData:
  LLM_API_KEY: "REPLACE_WITH_YOUR_API_KEY"
